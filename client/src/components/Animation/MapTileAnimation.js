import { TileLayer, LayersControl, WMSTileLayer } from "react-leaflet";
import React from "react";

const { BaseLayer } = LayersControl;

const MapTileAnimation = ({ baseLayerData }) => {
  return 1;
};

export default MapTileAnimation;
